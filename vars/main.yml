---
# vars file for ansible-torbutton

apt_dependencies:
  - zip

path: "{{ dirpath }}/{{ repo_name }}"

xpi_new: "{{ path }}/pkg/torbutton-{{ patched_version }}.xpi"
xpi_dest: "{{ tor_browser_bundle_path }}/Browser/TorBrowser/Data/Browser/profile.default/extensions/torbutton@torproject.org.xpi"
